SELECT 
    tr.record_id,
    tr.version_id,
    tr.patient_narrative,
    tr.ai_response,
    tr.ai_generated_at,
    tr.attachment_urls,
    tr.created_at,
    tr.updated_at
FROM triage_records tr
WHERE tr.record_id = '{{global.URL.queryParams.consultation_id}}'
ORDER BY tr.created_at DESC;